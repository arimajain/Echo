import SwiftUI

/// Lab tab - Structured tactile exploration.
/// Uses TextureLabContainerView with segmented control.
struct LabTabView: View {
    var body: some View {
        TextureLabContainerView()
    }
}
