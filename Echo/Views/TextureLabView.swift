import SwiftUI

/// Main entry point for Texture Lab.
/// Delegates to the new immersive container view.
struct TextureLabView: View {
    var body: some View {
        TextureLabContainerView()
    }
}
